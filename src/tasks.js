/* adds given task to the queue */
var addTask = exports.addTask = function(tasker, taskCb) {
    
};

/* removes given task from the queue */
var removeTask = exports.removeTask = function(tasker, taskId) {
    
};